asyncapi: '2.6.0'
info:
  title: Separated Same Msg
  version: '1.0.0'

channels:
  signupA:
    publish:
      operationId: ProducerA
      message:
        $ref: '#/components/messages/UserSignedUp'
  signupB:
    subscribe:
      operationId: ConsumerB
      message:
        $ref: '#/components/messages/UserSignedUp'

components:
  messages:
    UserSignedUp:
      name: UserSignedUp
      payload:
        type: object
        properties:
          userId: { type: string }