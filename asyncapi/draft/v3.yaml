asyncapi: 3.0.0
info:
  title: Order Service API
  version: "3.0.0"
channels:
  order/created:
    address: order/created
    messages:
      orderCreatedMessage:
        $ref: '#/components/messages/OrderCreated'
operations:
  sendOrderCreated:
    action: send
    channel:
      $ref: "#/channels/order/created"
    message:
      $ref: "#/channels/order/created/messages/orderCreatedMessage"
    operationId: orderService_sendOrderCreated
  receiveOrderCreated:
    action: receive
    channel:
      $ref: "#/channels/order/created"
    message:
      $ref: "#/channels/order/created/messages/orderCreatedMessage"
    operationId: paymentService_receiveOrderCreated
components:
  messages:
    OrderCreated:
      name: OrderCreated
      payload:
        type: object
